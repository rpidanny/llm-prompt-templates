import { llmPromptTemplates } from './llm-prompt-templates';

describe('llmPromptTemplates', () => {
  it('should work', () => {
    expect(llmPromptTemplates()).toEqual('llm-prompt-templates');
  });
});
